import './MarkedComponent.css';

export default class MarkedComponent {

  constructor () {
    console.log('in MarkedComponent constructor');
  }

  readyButton1(event) {
    var cmp = event.detail.cmp;
    this.button1Cmp = event.detail.cmp;
  }

  tapButton1(event) {
    this.button1Cmp.setText(new Date())
  }

}

import './TreeMapComponent.css';

export default class TreeMapComponent {

  constructor () {
    console.log('in TreeMapComponent constructor');
  }

  readyButton1(event) {
    var cmp = event.detail.cmp;
    this.button1Cmp = event.detail.cmp;
  }

  tapButton1(event) {
    this.button1Cmp.setText(new Date())
  }

}

/**
 * @class HasOne
 *
 * **This is not a real JavaScript class and cannot be created.  This is for documentation
 * purposes only.**
 *
 * This class provides for creating a keyless `OneToOne` association. This
 * declaration should be on "owner" entity. See `OneToOne` and
 * `Association` for more information.
 */

/**
 * @cfg {String} name
 * An alias for {@link Reference#role role}.
 */

/**
 * @cfg {String} model
 * An alias for {@link Reference#type type}.
 */
